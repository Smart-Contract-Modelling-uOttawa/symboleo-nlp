Domain test_dm
  PartyA isA Role;
  PartyB isA Role;
  Product isAn Asset;
  UnquarantineProduct isAn Event with agent: Role, product: Product;

endDomain

Contract test_cs(  )

Declarations
  shi farms: PartyA;
  partyB: PartyB;
  the product: Product;
  evt_unquarantine_product: UnquarantineProduct with agent := shi farms, product := the product;

Preconditions

Postconditions

Obligations
  ob_keep_quarantine: Obligation(shi farms, partyB, true, not Happens(evt_unquarantine_product));

Surviving Obligations

Powers

Constraints

endContract